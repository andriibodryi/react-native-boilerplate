import { StyleSheet } from 'react-native';

import Spacing from '@src/styles/constants/spacing';

export const INPUT_ICON_SIZE = 27;

export const styles = StyleSheet.create({
  icon: {
    marginHorizontal: Spacing.s040,
  },
});
